pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        //tentativa de adicionar o JitPack repository ao build file para Bar Chart
        maven { url 'https://jitpack.io' }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()

    }
}
rootProject.name = "App Plano"
include ':app'
